####################################################################################################

import numpy as np

####################################################################################################

a = np.array([[2,3,4],
              [5,6,7],
              [8,9,10],
             ])

b = np.array([[.2,.3,.4],
              [.5,.6,.7],
              [.8,.9,.10],
             ])

####################################################################################################

d = np.array([2,3,4], dtype='f')
print a/np.atleast_3d(d)
for i in xrange(a.shape[0]):
    print a[i] / d[i]

####################################################################################################

print np.sum(a*b, axis=1)
print np.einsum('ij,ij->i', a, b)
for i in xrange(a.shape[0]):
    print np.dot(a[i],b[i])

####################################################################################################

c = np.cross(a, b, axisa=-1, axisb=-1, axisc=-1)

print c
for i in xrange(a.shape[0]):
    print np.cross(a[i],b[i])

####################################################################################################
# 
# End
# 
####################################################################################################
